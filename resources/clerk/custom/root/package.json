{
  "devDependencies": {
    "shadow-cljs": "{{shadow-version}}",
    "gh-pages": "^3.2.3",
    "http-server": "^14.1.1"
  },
  "scripts": {
    "gh-pages": "gh-pages -d public --dotfiles true",
    "serve": "http-server -c-1 -o -p {{http-server-port}}"
  }
}
